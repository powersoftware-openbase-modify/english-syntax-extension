# 隐私政策

**生效日期**：2026-07-26 ｜ 适用于「英语拆解宝」Chrome 扩展全部版本与 IntelliJ IDEA 插件。

## 一句话结论

本项目**不收集、不上传、不出售任何用户数据**。开发者收不到你的任何信息——没有服务器，也没有分析或遥测代码。

## 数据去了哪里

两个运行时（Chrome 扩展 / IntelliJ 插件）都只有两个数据流向：

**1. 你的设备本地**

- **Chrome 扩展**：
  - **模型配置**（接口地址、API Key、模型名、自定义请求头、超时、各项开关）存在 `chrome.storage.local`。该存储被设为 `TRUSTED_CONTEXTS`，**网页里的内容脚本读不到它**，只有扩展自身的后台与设置页能访问。
  - **解析缓存**（句子原文与其解析结果）存在 IndexedDB，用于避免重复请求。可在设置页查看用量、清空或导出。
- **IntelliJ 插件**：
  - **API Key 存在 IntelliJ PasswordSafe**（系统级凭据存储），**不进入**插件设置文件、预览页面脚本、日志或缓存。
  - **解析缓存**存在本地 SQLite 数据库，格式与 Chrome 扩展的导出文件互通，可双向导入导出。

这两类数据都不会离开你的设备，除非你自己使用导出功能。

**2. 你自己配置的模型服务**

只有当你主动触发解析时，才会把**待解析的英文句子文本及其分词结果**发送到**你自己填写的接口地址**，同时发送你填写的 API Key（作为认证凭据）。使用「纠错重新解析」功能时，你手写的反馈文本也会一并发送。

**不会发送**：网页地址、Cookie、表单内容、浏览历史、你的身份信息，或页面上未被解析的任何其他内容。

> 这个接口地址由你指定——可以是本机运行的 Ollama（数据完全不出设备），也可以是 DeepSeek、OpenAI 等第三方服务。**如果你填的是第三方服务，这些句子将受该服务的隐私政策约束，与本项目无关。请自行确认你信任该服务。**

## 权限用途（Chrome 扩展）

| 权限           | 用途                                                                           |
| -------------- | ------------------------------------------------------------------------------ |
| `activeTab`    | 仅在你点击扩展图标、使用右键菜单或快捷键时，临时访问当前标签页以读取待解析段落 |
| `scripting`    | 把解析卡片注入当前页面                                                         |
| `storage`      | 本地保存模型配置与偏好                                                         |
| `contextMenus` | 提供「解析选中文本」「解析此区域」右键菜单                                     |
| 可选主机权限   | 访问你配置的模型接口。**默认不申请**，仅在你保存配置时针对该地址按需申请       |

扩展**没有申请常驻的网页访问权限**：不会在后台读取你浏览的任何页面。（IntelliJ 插件不申请任何系统权限，只在 IDEA 内的 Markdown 预览里工作。）

## 安全措施

- API Key 与自定义请求头**从不进入日志或渲染结果**。Chrome 侧：若模型响应中意外混入了凭据，会在送回页面前被替换为 `[redacted]`；IntelliJ 侧：Key 只经 PasswordSafe 存取，发往预览页面的消息经键白名单过滤，含凭据字段的消息整体拒绝。
- 模型返回的译文**按纯文本渲染**，不会作为 HTML 解析，因此恶意响应中的脚本不会执行。
- 两个运行时都**不加载、不执行任何远程代码**，全部逻辑随安装包一同分发。

## 儿童隐私

本项目不面向 13 岁以下儿童，也不会有意收集其信息（实际上不收集任何人的信息）。

## 政策变更

政策若有变更，会更新本文件并在 [CHANGELOG.md](CHANGELOG.md) 中说明。本文件的完整修改历史可在 Git 提交记录中查阅。

## 联系方式

疑问或担忧请提 [Issue](https://github.com/javaside/english-syntax-extension/issues)；涉及安全的问题请走 [私密报告渠道](https://github.com/javaside/english-syntax-extension/security/advisories/new)。
