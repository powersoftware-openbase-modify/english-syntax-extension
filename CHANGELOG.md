# 更新日志

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 1.3.3 — 2026-09-02

这一版修复成分划分里三类会直接显示给用户的错误：定语从句只标引导词、名词后的介词短语误标状语、系表结构两种口径混用。三类都**完全通过**此前的全部本地校验，不进修复轮就写进缓存长期显示。

**升级后请重新加载扩展并刷新页面；IDEA 插件需要安装新版 zip。** `CORE_PROMPT_VERSION` 升为 `10`，已有 core 缓存会自动失效并按新规则重新分析；详解缓存不受影响。

### 修复

- 定语从句不再被切开。此前 `The Spring AI project aims to streamline the development of applications that incorporate…` 里 `that` 被单独标成定语从句，从句自己的谓语与宾语平铺成主句的同层成分，页面上出现两个同级「谓语」，而引导词底下挂着整个从句的译文。提示词现在写明从句从引导词一直延伸到从句自己的宾语与状语，省略引导词的从句同样整块标注。
- 紧跟名词短语的介词短语归 `ATTRIBUTE`，不再误标 `ADVERBIAL`。`the development` + `of applications` 现在是宾语 + 定语，译文也不再退化成单字「的」。此前 `PREPOSITIONAL_PHRASE_RULE` 把 `ATTRIBUTE` 限死成「名词短语内部的修饰语」并只给前置修饰的例子，与黄金集 conventions 的「可前置也可后置」矛盾，后置介词短语于是无处可归。量词与部分结构（`a lot of`、`some of`、`no amount of`）没有例外；已经嵌在另一个介词短语里的不再拆。
- 系表结构定死为「`PREDICATE` 只含系动词、补足部分标 `PREDICATIVE`」。此前 `PREDICATE_SCOPE_RULE` 举例 `"is independently deployable" is one PREDICATE`，而同一份规则清单里的 `PEER_COMPONENT_RULE` 禁止 `PREDICATE` 吸收 `PREDICATIVE`，同一句在两次调用之间会在两种切法之间跳。被动、完成、进行时的 be/have 仍留在动词组内。
- 成分译文必须覆盖整段内容而不是只译中心词：`incorporate artificial intelligence functionality` 要译成「整合人工智能功能」而不是「整合」。

### 校验

- 双端 validator 新增三条本地硬门（TS/Kotlin 逐条、逐文案一致，错误原样进入 repair prompt）：五类从句角色的成分不得只有 1 个 lexical word；`ATTRIBUTIVE_CLAUSE` 的下一个成分不得是 `OBJECT` / `PREDICATIVE` / `COMPLEMENT`；成分不得以「几乎不可能悬垂」的介词收尾（`of/into/onto/upon/within/among/between/despite/during/toward/towards`）。`for`/`with`/`at`/`from`/`to` 刻意不收——关系从句的介词悬垂让它们合法地出现在成分末尾。
- 主句谓语与主句状语跟在定语从句后面仍然合法（`I met the man who called yesterday in the park.`），三条新硬门在已保存的 49 句真模型预测上重放零误拒。

### 黄金集

- **更正 1.3.1 的说法**：当时并入的 32 句（`auto-gen-*` / `retry-*` / `improved-*`）不是「人工复核标注」，而是模型批量生成后只跑结构校验就收编的。这次逐句复核出 15 处错标并全部修正：`that` 单独当定语从句、`developers` / `you` 当主语从句、`near the frontier of` 与 `by adopting` 介词悬空、动名词短语被切成两个同级宾语、`write many fast unit tests` 整块当谓语、系表两种口径混用。
- 移除 `auto-gen-003`（`It's something you build in…`）：分词刻意不拆缩写，整个 `It's` 是一个 Token，主语与系动词无法分到两个成分里。
- 新增本次报告的真实文档句 `doc-of-phrase-clause-1`，它同时覆盖「名词 + of 短语后置定语」与「完整定语从句」。conventions 里「刻意排除名词后接介词短语」这条随之作废——那是技术文档里最高频的结构，排除等于测不到。
- conventions 新增 5 条口径（从句整块、系表拆开、of 短语归属、非限定动词短语、缩写句不纳入），并新增一条机器断言钉住 of 短语不得藏在名词短语成分里。

### 测试

- 黄金集真模型评测重建了基线（deepseek-chat，81 句，`temperature 0`，无失败句）：整句 exact 70.37%、span exact F1 0.8780、labeled span F1 0.8727、exact-span role accuracy 99.40%。在与 8-30 基线共有的 49 句上做同尺子对比，整句 exact 从 67.35% 升到 85.71%（+18.37 pp），labeled span F1 从 0.8058 升到 0.9343（+12.84 pp）——这段增益覆盖 `CORE_PROMPT_VERSION` 8 → 10 的累积改动（1.3.2 的两轮 repair 与本版的四条提示词修复）；1.3.2 发布时没有重跑评测，两者无法再拆分。
- 新并入黄金集的 32 句真实新闻与技术文章句明显更难（labeled span F1 0.7927 对教学句的 0.9343），81 句的整体数字才是可用的准确率参照。
- 把这 81 句的真模型输出重放过新硬门：`auto-gen-027` 的介词悬空与 `retry-008` 的定语从句被切开各命中一次，即这两类坏划分现在会进修复轮而不是直接渲染。
- Chrome 903 个单元测试与 34 个 Playwright E2E 全部通过；2 个商店截图用例按配置跳过，lint 保持唯一既有基线错误。
- IntelliJ Web 74 个测试与 Kotlin 269 个测试全部通过；插件构建与项目配置校验通过。

## 1.3.2 — 2026-09-02

这一版修复模型输出校验与修复链路中的误拒和失败回显问题，并让 IntelliJ IDEA Markdown 预览正确识别 `<HARD-GATE>` 等自定义标签中的英文内容。

**升级后请重新加载扩展并刷新页面；IDEA 插件需要安装新版 zip。** `CORE_PROMPT_VERSION` 升为 `9`，已有 core 缓存会自动失效并按新规则重新分析；详解缓存不受影响。

### 修复

- 从双端介词硬门白名单移除可独立作副词的 `throughout`，不再把 `uses tools throughout` 之类的合法分析误送进无意义的修复轮。
- core repair 从一轮扩展为至多两轮，并且每轮只携带仍失败的句子、最新非法 JSON 和对应错误；已经修好的同批句子不再回流。
- 修复提示明确要求在限定词前切开被谓语吞入的名词短语，并逐条自检校验错误，提高结构错误的修复成功率。
- IntelliJ 失败卡现在始终携带并保存原句 Token；即使没有收到任何流式分片，也能按源序显示原句、错误提示和重试入口。
- IntelliJ 自动扫描与悬停快捷键支持标签名含连字符的 Markdown 自定义容器。`<HARD-GATE>`、`<EXTREMELY-IMPORTANT>` 等元素即使被 Chromium 默认渲染为行内元素，也能作为明确内容边界解析；普通 `span` / `em` 与安全排除规则保持不变。

### 测试

- Chrome 894 个单元测试与 34 个 Playwright E2E 全部通过；2 个商店截图用例按配置跳过，lint 保持唯一既有基线错误。
- IntelliJ Web 74 个测试全部通过；Kotlin 测试、插件构建与项目配置校验通过。

## 1.3.1 — 2026-08-30

这一版修复 1.3.0 合并后暴露的构建与契约回归，并把新增的 32 条人工复核标注正式纳入黄金集。

**升级后请重新加载扩展并刷新页面。** `CORE_PROMPT_VERSION` 升为 `8`，已有 core 缓存会自动失效并按一致的新提示词重新分析；详解缓存不受影响。

### 修复

- 清理误提交到正式测试目录的联网评估、生成脚本和依赖本地 `test-output/` 的临时测试，恢复 CI 与 `npm run package`。
- 恢复黄金标注 fixture 的 `conventions` 与 `id` 契约，保留并验证新增 32 条标注；当前 81 条黄金标注全部通过生产 tokenizer、覆盖规则和 validator。
- 彻底删除双端 core/repair 提示词里要求输出已废弃 `COORDINATE_CLAUSE` 的残留旧指令。并列句现在与 validator 一致地按 `SUBJECT` / `PREDICATE` / `OBJECT` 等同层成分平铺，只把 FANBOYS 并列连词标为 `CONJUNCTION`。
- 更新并列句假模型与 E2E 契约，避免测试绕过生产 validator 后继续断言废弃结构。
- 为相邻谓语中的助动词/情态动词拆分提供更明确的修复提示，并在 core prompt 中明确完整动词组范围。

### 测试

- Chrome 单元测试、Playwright E2E、lint 基线、格式检查与扩展打包全部通过。
- IntelliJ Web 测试、Kotlin 测试、插件构建和项目配置校验全部纳入发布前门禁。

## 1.3.0 — 2026-08-30

这一版的核心改进是**废弃 COORDINATE_CLAUSE**，并列句从「几个巨大色块、每块挂一整句中文」改为**同层成分平铺**（subject/predicate/object 等），配合四条新增的本地硬门，大幅提升了真实散文的成分划分准确性与显示效果。

**升级后请重新加载扩展并刷新页面。** 成分划分方案这一版彻底改变（`CORE_SCHEMA_VERSION` 4 / `CORE_PROMPT_VERSION` 6），**已有的 core 缓存随之作废**，下次打开文档会重新分析一遍；详解缓存不受影响。

### 重大变更

- **废弃 COORDINATE_CLAUSE，并列句按同层成分平铺**。旧方案把并列句整体标成 2-3 个 `COORDINATE_CLAUSE`，每块挂一整句中文译文，在真实散文里退化为「巨大色块」而非成分标注。新方案要求：只有各分句自带主语、由 FANBOYS（for/and/nor/but/or/yet/so）或分号连接时才算并列句；其他情况（逗号串起的祈使句、共享主语的并列谓语）一律按同层成分平铺（各分句的 subject/predicate/object 等作为句子的顶层成分），并列连词单独标 `CONJUNCTION`。这样卡片才能显示成分划分而不是几整块译文。本地硬门（第 9 条）会拦截违反新约定的输出，不再只写在提示词里。参见验证报告的对比示例。

### 新增

- **四条本地语法硬门（两端）**：提示词里能本地判定的粒度规则，现在变成硬校验。只写在 prompt 里的约束等于没有约束：模型违反了没人拦，坏划分照样写进缓存并长期显示。这几条都只看「成分序列 + Token 文本」就能判，不需要句法分析器，而且判错的代价可控——失败只是走一次已有的修复轮。错误文案本身就是发给模型的修复指令，所以必须写成「该怎么做」而不只是「哪里错」。
  - **硬门 1：谓语不得以主格代词开头**（i/you/he/she/it/we/they）。英语的动词组绝不可能以它开头，命中即说明主语被吞进了谓语。实测 DeepSeek-chat 把 "She kept practicing until…" 整句标成 `PREDICATE` + `ADVERBIAL_CLAUSE`，一个主语都没有，而旧的四条硬门一条都拦不住。用「谓语开头」而不是「整句缺主语」判定，是因为祈使句本来就没有主语。
  - **硬门 2：谓语不得以限定词开头**（the/a/an/this/these/those 等）。限定词是名词短语的左边界，命中即说明主语被吞了进来。
  - **硬门 3：谓语内部不得含限定词**。动词组内部出现它，说明宾语/表语/补语被吞了进来（`PEER_COMPONENT_RULE` 要挡的正是这个，但此前只写在提示词里）。
  - **硬门 4：从属连词引导的是从句，不是并列分句**（because/although/if/when/while 等）。`COORDINATE_CLAUSE` 的首个实词命中从属连词，且整句没有 `CONJUNCTION` 时触发——有 CONJUNCTION 不判是因为 "Because A, B, and C" 里第一个并列分句本来就以从属连词开头。
- **新增黄金标注测试集（1368 行）**：`shared-fixtures/core-gold-annotations.json` 收录了基础句型、从句、被动、祈使、并列等各类句型的标准答案，其中带 `doc-` 前缀的句子逐字取自真实技术文档（code.claude.com/docs 的 Claude Code 概览页），用来测真实输入分布。精选教学句的句长与句式都比实际网页窄，单靠它无法判断线上准确率。
- **核心评估框架**：`chrome-plugin/scripts/core-evaluation.mjs` 与 `core-evaluation-runner.mjs` 提供离线评估能力，可用黄金标注批量测试分词与覆盖不变量，而不必每次都跑真模型（耗时且不稳定）。评估 validator 改动时用离线重放即可。

### 修复

- **单成分包住整句不再放行**。一个成分覆盖整句全部实词 = 没有划分。旧规则只认 `COORDINATE_CLAUSE`，换成 `SUBJECT` 或 `PREDICATE` 就一路通过。现在单成分包住整句（≥3 个实词）时触发硬门，强制拆分。
- **相邻 PREDICATE 必须合并**。并排的动词属于同一个谓语，两个 `PREDICATE` 不得相邻（除非中间有其他成分）。
- **介词不得独立成分**。介词与它管辖的一切是一个成分（`PREPOSITIONAL_PHRASE_RULE`），介词不得单独标一个成分。

### 变更

- **收紧并列分句判据**（第 9 条硬门，见上）：逗号、冒号、破折号本身不构成并列句，共享主语的并列谓语也不是并列句。
- **契约版本更新**：`CORE_SCHEMA_VERSION` → `4`（成分划分方案已彻底改变），`CORE_PROMPT_VERSION` → `6`（废弃 COORDINATE_CLAUSE 及新增硬门规则）。**本次升级会作废已有 core 缓存**，下次打开文档会重新分析一遍。
- **两端提示词与验证器同步**：IntelliJ 插件的 Kotlin 实现已同步废弃 COORDINATE_CLAUSE 及新增四条硬门，与 Chrome 扩展行为完全一致。

### 测试

- 新增 452+ 行分析验证器测试（`analysis-validator.test.ts`）
- 新增 128+ 行分句器测试（`segmenter.test.ts`）
- 全量测试通过率：896/896（41 个测试文件）
- 真实文档验证：Claude Code 文档页面分句准确率 100%

## 1.2.0 — 2026-08-27

这一版有两件大事：仓库多了**第二个运行时**——IntelliJ IDEA Markdown 预览插件，与 Chrome 扩展共用同一套契约与缓存，同一批文档两端阅读零重复请求；以及一轮真机验收（真 Chrome + 真 DeepSeek）把一批只在真环境里才显形的缺陷逼了出来，连同两端提示词口径一起收口。

**升级后请重新加载扩展并刷新页面。** 成分粒度口径这一版改了（`CORE_SCHEMA_VERSION` 3 / `CORE_PROMPT_VERSION` 5），**已有的 core 缓存随之作废**，下次打开文档会重新翻译一遍；详解缓存不受影响。

**本版 Release 附两个包**：Chrome 扩展 `english-syntax-extension-v1.2.0.zip`（解压后加载）与 IDEA 插件 `intellij-plugin-1.2.0.zip`（不解压，`Settings → Plugins → ⚙ → Install Plugin from Disk…`，需 IDEA 2025.1+ 与自带 JCEF 的 JetBrains Runtime）。两端从此同版本号发布。

### 新增

- **IntelliJ IDEA Markdown 预览插件（`intellij-plugin/`）**：本仓库第二个运行时。在 IDEA 自带的 Markdown 预览里做与 Chrome 扩展一致的句法拆解——`Tools → 句法学习` 开始后，预览中可见及附近的英文段落替换为三层对齐卡片，点击成分看详解，停止即完整恢复原文。要点：
  - **不自建预览**：复用 IDEA 官方 JCEF 预览（`MarkdownJCEFHtmlPanel`），不注册 provider、不反射访问其内部，用户无需切换任何设置；
  - **API Key 存 PasswordSafe**：不进插件状态文件、预览页脚本、桥消息、缓存或日志；发往预览页的消息在入口做键白名单过滤，含 `apiKey`/`headers`/`baseUrl` 的消息整体拒绝（`SecretIsolationTest` 钉住）；
  - **SQLite 缓存与 Chrome 扩展互通**：缓存键与交换格式跨端一致（`shared-fixtures/` 契约向量双端测试钉住），两端阅读同一批文档零重复请求；
  - **模型链路同骨架重实现**（Kotlin）：prompt 序列化、输出校验、一次修复、能力降级（JSON Schema / 流式 / 思考控制只持久化否定态）、五档优先级调度、按句缓存，与 Chrome 端行为对齐；
  - Markdown 预览重渲染（编辑保存后）自动换代重扫，旧响应不污染新 DOM（Kotlin/JS 双端 generation 守卫）；
  - **快捷键按段翻译**（默认 `Alt+T`，可在 IDEA Keymap 改键）：鼠标停在预览的某一段上按下即只解析那一段，会话未启动时自动轻量启动——不再只有「整篇翻译」一种模式，长文档不必为了看一段付出上百次模型请求。焦点在 JCEF 预览里时 IDEA 的按键可能不上交 IDE，预览页自带 keydown 兼底通道且键位跟随 keymap；鼠标不在正文上、或停在已翻译的卡片上，右下角浮层会给出明确提示（快捷键没有菜单反馈，静默失败会让用户以为键坏了）。
  - **设置页显示缓存用量并支持一键清空**（与 Chrome 端设置页对齐）：显示已缓存条目数与估算占用，清空前弹确认对话框，清完把清掉的条目数回报在状态行上。缓存实例从 `AnalysisServiceService` 单独暴露，设置页不必为了查用量把整条分析链路拉起来。

### 修复（IntelliJ 插件）

- **点成分后详解面板先落在整句末尾、等内容回来才跳到成分所在行；且短句会「本来一行、点一下变两行」**。两个症状同一处成因：加载占位面板走的是 `sentence.after(panel)` 并**无条件**给句子加 `english-syntax-has-detail`，最终面板却走按点击行定位的 `#placeDetailPanel`——占位与终态两套落点，内容一到必然跳一次（这条行判定曾修好过，占位那条路径是后来加的，没跟着走同一套判据，于是老 bug 以「先在句尾、后跳回来」的形式复发）。而 `has-detail` 会让句子变块级、撑满整栏，本来与它共行的邻句被挤到面板下方，看着就是「一行变两行」。现两条路径共用同一个 `#anchorDetail`：**句内还有位于点击行下方的成分**时（长句折行、点的不是最后一行）才插在该行最后一个成分之后并加 `has-detail`；否则插到**与点击行共行的最后一句之后**，且绝不加 `has-detail`（与 Chrome 端 `.sentence:has(.detail)` 只在面板真在句内时命中的语义一致）；`#closeAllDetailPanels` 顺带清掉残留的 `has-detail`。`render.test.ts` 新增两条用例（模拟 `getBoundingClientRect` 造出行布局）钉住「占位与终态同一落点」和「共行短句不被顶下去」。
- **同一段按第二次快捷键后永远停在「解析中」，且已翻好的句子点不动**。三处成因叠在一起：① 隐藏原文的 CSS 规则（`[data-english-syntax-hidden]{display:none!important}`）整条缺失，一段翻完屏上有两份文字，鼠标本来就停在那份可见原文上；② 原文是卡片的**兄弟**节点，页面侧「已解析」判据只查了 `closest([data-english-syntax-card])`，拦不住停在原文上的第二次按键，于是对同一段重复下发 `PARSE_BLOCK`；③ Kotlin 侧 `parseExplicitBlock` 遇到「这段已全部 READY、没有新句可派」时静默返回——而页面在按下的那一刻就自己打上了「解析中」竖条与浮层，撤掉它的唯一信号是该块的 `CORE_RESULT`/`CORE_ERROR`，于是标记永不消失。重复下发还顺带让 `renderer.registerBlock` 清空该块的句子映射，卡片仍在 DOM 上、监听器也在，但点成分查不到句子，表现为「原来翻译出来的句子也点击不了」。现补上 CSS 规则、页面侧按 `data-english-syntax-hidden` 也提示「该段已解析」、Kotlin 侧改为把已存结果经同一个 `applyOutcome` 原样重发（不调模型、不构成重扫环）。`preview.css` 规则、页面双判据、Kotlin 重发三处各有守护测试。
- **点「停止并恢复原文」后再点开始，整页不再翻译**（无卡片、无报错）。渲染器的 `#sentences` 是全局 sentenceId 映射，`restoreAll()` 与 `registerBlock()` 都没清它；而「停止 → 再开始」这条路径上 blockId 由元素上的 `data-english-syntax-block` 沿用、sentenceId（`s-{blockId}-{index}`）也照旧复用，新旧条目精确相撞：`#ensureSentence` 判「这句已存在」提前返回，新 `BlockRecord.sentences` 永远拿不到这一句，`#repaintBlock` 于是算出 `hasContent=false` 并走 `#restoreBlock`——卡片一张都画不出来。现 `registerBlock()` 换记录时连带删除旧记录名下的 sentenceId。官方 `updateDom` 重渲染不会踩（整个 body 被换掉，blockId 全部重新分配），所以此前只有停止→再开始这一条路径失效。`render.test.ts` 与 `PreviewSessionTest` 双侧钉住。
- **翻译卡片的英文行顺序与原文不一致**。卡片句子排列此前按「消息到达顺序」累积（`#blockSentenceOrder`），流式分片按模型输出到达、可能先吐后半句，final 卡片就按乱序排。现渲染前按 `sentenceId` 末尾的 `index`（源序）数值升序重排，无论分片/结果乱序与否都恢复原文顺序。新增 `render.test.ts` 用例钉住。
- **点「停止并恢复原文」后，预览页右下角又亮起「正在解析」进度浮层**。清卡（`RESTORE_ALL` → `renderer.restoreAll()`）也是 DOM 变更，会被 MutationObserver 的「卡片从有到无」边沿误判成官方 `updateDom` 重渲染，上报 `PREVIEW_RENDERED` → Kotlin 换代重扫 → `rescan()` 又 `setStatus("正在解析…")`。现于 `RESTORE_ALL` 分支同步把「有卡片」基线复位为 false，让我方清卡不构成重渲染边沿。新增 `bootstrap-lifecycle.test.ts` 钉住。
- **点「开始句法学习」后预览毫无变化、也无报错**。「移除自建预览面板」重构时把 JS→Kotlin 的消息接线整体丢掉了：`VISIBLE_BLOCKS`/`DETAIL_REQUEST`/`RETRY_SENTENCE` 无任何消费者，每层单测全绿但端到端无一条页面消息到达会话。现由 `PreviewSessionConnector` 统一收口接线（先接线后启动会话，顺序不可拆），并新增 `PageMessageWiringTest` 走 Panel 桥接入口钉住这条链路。
- **卡片永不渲染**：`CORE_*` 消息携带 `blockId`，JS 侧按消息惰性注册句子（此前生产路径从不注册，渲染器查不到句子直接 return）。
- 设置页保存异常不再冒泡成 IDE 错误弹窗；SQLite 驱动未注册导致的 Action 静默失败；预览面板定位改为经 `MarkdownPreviewFileEditor.PREVIEW_BROWSER` UserData（面板不是 FileEditor 本身，直接强转永远 null）。

### 修复（Chrome 扩展）

- **模型少吐一个收尾括号，整批句子不再全军覆没**。同一条 `ANALYZE_CORE` 覆盖最多 6 句，而 `JSON.parse` 失败此前直接抛 `INVALID_MODEL_OUTPUT`：正文里前几句往往是完整的，也照样被判死，且**修复轮压根不会跑**（异常在它之前就冒出去了）。撞上 `max_tokens` 断在半句上更是常态。现按截断先救一遍（`lenient-json.ts`：补齐未闭合的字符串/数组/对象，丢掉不完整的最后一项），救出来的对象交给既有的逐句校验；连救都救不回来时也不再判死整块，而是当成「这一批全无效」交给修复轮再要一次。网络 / 鉴权 / 超时 / 取消照旧上抛——那类失败重发一次也是白发，且 `AUTH_FAILED` 必须原样传到 SW 去暂停 profile。救援向量落进 `shared-fixtures/truncated-json-salvage.json`，两端共用。
- **一句同时只在飞一趟**。实测一句失败要付**三次**上游调用：首轮、视口重复回调补发的一趟、SW 回收后重连补发的一趟；后两趟还会 `++operationVersion` 把先到的响应整条作废，用户反倒从头再等一轮。现以块为哨兵（块是下发单位，每句只属于一个块）：`BlockRecord.inFlight` 记着在飞那趟，`analyzeBlocks` 只下发没有在飞的块，其余改挂等待链，落地后重走终态闸门决定还要不要补发；同批重复出现的块只算一份；重连计划也不再重入（一次 SW 回收常连着来好几个断开事件，每跑一遍都会补发一次）。
- **SW 被回收后账本要么被覆盖、要么忘不掉**。两个方向的错都出在「读 `activeTabs` 时回填还没落地」：① 页面重连时 `onConnect` 抢在回填前写账本，把「进行中」覆盖成 `stopped`——页面上卡片一张没少，弹窗却回落成「开始学习」；② 陈旧端口的断开事件会取消刚用新端口接上的那条会话；③ 标签页关闭 / 导航时账本还空着，清理无从下手，紧接着回填又把陈旧记录塞回内存，这个标签页**再也忘不掉**（旧 `documentId` 一直顶着，页面新会话的状态中继全被判成过期文档）。现读账本做决定的地方一律先 `await hydrated`；而「忘掉一个标签页」反过来必须**同步**做完（推到回填之后的话，紧随导航而来的状态中继会先看到旧 id 而被拒，新会话再也登记不上——SPA 导航正是这个次序），回填未落地时另立墓碑让回填跳过它，SPA 那一路还用存下来的 `documentId` 补发 `STOP_SESSION`。
- **从弹窗触发「解析选中文本」/「解析悬停段落」会让同一个标签页跑两套会话**。扩展页面不知道页面真实的 `documentId`，一律送占位值 `popup-tab-${tabId}`；这两条分支照它原样转发，而页面路由按 `documentId` 认 controller——于是又建一套：卡片与上游调用都翻倍，新那套的状态中继还因与账本不符被判 `REQUEST_CANCELLED`。同时它们此前完全不记账，SW 眼里这个标签页始终是停止：弹窗回落成「开始学习」，「解析此区域」与「重新解析可见段落」一律按停止态拒掉。现与其他会话分支一致：解析出真实 id 再转发，并把这次手势记进 `activeTabs`（只把 `stopped` 翻成 `running`，已在跑 / 已暂停的原样保留）。
- **进度胶囊在开始的瞬间闪一下「✓ 解析完成」**。完成口径是「当前没有在飞的工作」（屏外句子要滚到可见才入队，按全覆盖要求长页面会永远停在「解析中…」），但它挡不住派发**之前**：扫描登记完、视口回调还没回来时 `queued` 与 `inFlight` 都是 0。而 `cache-check` 相位不在任何计数里，`transition()` 每换一次相位就上报状态，于是又多一条假空闲状态。现完成还要求「至少落地一句」（ready + failed + skipped > 0），且状态里的 `inFlight` 与块级在飞判据共用同一张相位表。
- **没有 doctype 的页面上快捷键全线失灵**（报「未找到可解析的段落」，同一份内容加上 doctype 就正常）。这类页面 `document.compatMode === "BackCompat"`，Chrome 保留旧的 [hover/active 怪癖](https://quirks.spec.whatwg.org/#the-active-and-hover-quirk)：裸 `:hover` 只让链接匹配，`querySelectorAll(":hover")` **整页恒为空集**。而这条怪癖只在「复合选择器里除伪类之外别无他物」时生效，把它塞进 `:is()` 就落进子选择器语境、不再适用——现两端一律查 `:is(:hover)`（实测同一 quirks 页面同一位置：`:hover` → 空集，`:is(:hover)` → `html > body > main > p#safe`；标准模式下两者恒等，不必先探文档模式）。IntelliJ 预览页同改：那份 HTML 由 IDEA 生成，doctype 有无不由插件说了算。内容脚本另记最后的指针坐标（capture + passive，页面 `stopPropagation` 挡不住），`elementFromPoint` 只作末位兜底——它救不了这条缺陷：快捷键常常是**冷启动**，内容脚本正是被这一按注入的，此后指针不动就永远等不到第一个 pointer 事件。
- **选中一段已翻好的文字再「解析选中文本」，同一段出现两张卡片，停止后原文回不来**。选区路径此前无条件新造一条 `selection-N` 记录，而元素的块 id 由 `WeakMap` 认定、始终是同一个：同一个宿主于是被两条记录各渲染一张卡，两张卡还各自把它 `display:none`，谁 `restore()` 都还不回原样。现一个元素只许有一条块记录——已登记就复用那一条（按它自己的整段文本重来，不改写在飞记录的句子），只有跨段选区或落在没有安全块的位置才另造临时锚点。顺带把「解析选中文本」「解析此区域」的失败也接到页面胶囊上：SW 会丢弃页面命令的响应，右键菜单只反馈「已触发」，此前这两个手势的「该段已解析」「未找到可解析的段落」全是静默的。
- **详解标注把同一片段重复上屏**（英文重复、区间嵌套，圈号解释列表也重复描述同一批词）。`structures` 只是自由数组，JSON Schema 约束不了跨项区间关系，模型会同时返回「整段」和它内部的每个词组。现双端 `validateDetail` 要求每项完全位于点击 focus 内、按 Token ID 升序且互不重叠，流式预览用同一套过滤规则，提示词也明确禁止「先返回整段再重复拆内部」。core 流式分片同时补上「纯标点不成为成分」的判据——按源 Token 的 `punctuation` 标记判定，不信模型给它的角色。
- **面向用户的错误文案全部改中文**，并把「消息不合协议」从 `INVALID_MODEL_OUTPUT` 里分出去（新错误码 `MALFORMED_MESSAGE`）。此前扩展内部消息形状不对时报的是「模型输出无效」，把人引向「换个模型试试」，而真正的原因与模型无关。
- **同一段再按快捷键：不再误报「未找到可解析的段落」，也不再把同一段解析两遍**。段落被卡片替换后原文已隐藏，鼠标停在的其实是卡片，而卡片文字在 Shadow DOM 里——段落定位一路向上找不到候选，于是第二次按 `Alt+T` 报的是与事实相反的「未找到可解析的段落」。现在分情形明说：已解析完的提示「该段已解析」（有失败句时带上句数，可点卡片里每句的「重新解析」），仍在解析的提示「该段正在解析中…」。同时补上「在飞不重复下发」与同段 400ms 去抖：此前趁第一次解析还没回来连按两次，会为同一批句子再发一条模型请求，且让先到的那条结果整条作废——白付一次调用，用户还得从头多等一轮；连按恰好落在算 sentenceId 的异步窗口里时更会重复注册段落，卡片留在页面上却没人认领（点成分无反应）。快捷键没有右键菜单那样的「已触发」反馈，所以「这一按没有下发」的每一种情形都必须回一句提示，静默返回会让用户以为键坏了。

### 变更

- **成分粒度修正（两端）**：一句话被切成一串「谓语」的问题。core 提示词此前只有「谓语不许吞掉宾语」这类**下界**规则、没有上界，指令型文本因此被切到词级——实测 `Help turn ideas into fully formed designs and specs through natural collaborative dialogue.` 得到 8–9 个成分（`Help`/`turn` 两个谓语、介词与其宾语拆开、宾语短语误标定语），6 个祈使动词逗号串成的一句得到 16 个成分 / 6 个谓语。现补三条边界并写死判定顺序：**先定分句层级**（各带谓语、能独立成句的分句一律整块 `COORDINATE_CLAUSE`）、`PREDICATE` 只覆盖动词组本身（`Help turn` 是**一个**谓语，两个谓语不得相邻）、介词短语连同并列宾语是**一个**成分（动词或介词管辖的名词短语永不是定语）；`PEER_COMPONENT_RULE` 同时收窄为「在单个分句之内」，两条规则不再互相打架（平列时实测同一句会在两种切法之间跳）。同一模型同一句实测结果：前者 4 个成分 / 1 个谓语，后者 12 个成分 / 0 个谓语（整串按并列分句），输出 token 顺带降 15%–30%。
- **修复轮不再丢规则**：`buildRepairPrompt` 此前只带 peer + supplement 两条，覆盖率、角色枚举、并列/复合/简单句与译文要求全丢——一句进了修复轮，剩下的唯一语法指导就是「把成分拆开」，实测越修越碎。现与首轮共享同一份 `CORE_ANALYSIS_RULES`。
- **提示词版本进缓存键**：`CORE_PROMPT_VERSION` 升为 `5`，且 core 键带 `CORE_PROMPT_VERSION`、detail 键带 `DETAIL_PROMPT_VERSION`（两条提示词各自演进，改 core 规则不作废已有详解）。改提示词不再把旧粒度的结果永久钉在缓存里与新结果混着显示。**本次升级会作废已有 core 缓存**，下次打开文档会重新翻译一遍。同批调整的契约版本号：`DETAIL_PROMPT_VERSION` → `4`（详解 focus/重叠规则）、`CORE_SCHEMA_VERSION` → `3`（成分粒度口径已变，旧结果一律作废）；交换格式的字段形状未变，两端与 `shared-fixtures/` 向量同步。
- **两端提示词逐字对齐上门禁**：整段 core 提示词落进 `shared-fixtures/core-prompt-parity.json`，TS 与 Kotlin 双端断言逐字相等——此前只靠人肉同步，一边改了另一边没改不会有任何测试红。
- **仓库重组为双子模块结构**：Chrome 扩展的全部源码、测试、脚本与配置迁入 `chrome-plugin/`（完整自治 npm 工程），与 `intellij-plugin/` 平级；仓库根只保留双端共享的东西——`shared-fixtures/` 契约向量、`docs/architecture/` 架构文档、`CHANGELOG.md` 与各根文档。对使用者无任何影响（构建产物、功能、发布流程均不变），源码路径全部前移一层。开发者注意：Chrome 侧 npm 命令现在在 `chrome-plugin/` 里跑；`intellij-plugin/` 有了自己的 `package.json`，web 桥测试改在子目录里独立运行。
- **隐私政策与安全策略覆盖 IntelliJ 插件**：`PRIVACY.md` / `SECURITY.md` 原本只描述 Chrome 扩展，现补充 IntelliJ 侧的数据流（API Key 存 PasswordSafe、SQLite 缓存、与 Chrome 扩展的缓存互通）。不改变任何实际行为，只是把已存在的实现如实写进文档。

## 1.1.0 — 2026-08-01

这一版围绕「该翻的都翻到、翻得更快、状态看得懂」三件事。**升级后请重新加载扩展并刷新页面。**

### 新增

- **正在解析的段落会自己亮出来**。此前只有右下角进度胶囊给一个全局计数，段落本身从按下快捷键到首个成分出现（可能数秒）没有任何变化，进度对不上具体哪一段。现在解析中的段落显示一根左侧竖条与极淡底色，流式渲染换成卡片后标记跟着走，成功、失败、停止、断连——任何一种收尾都立即撤掉。标记用 `inset box-shadow` 实现，不参与布局计算，文字不会因此位移；系统开启「减弱动态效果」时不做呼吸动画。

### 修复

- **大片正文没有译文**。判断「这是不是一段」此前只看标签名，而 Mintlify 一类文档站（包括 Claude Code 自己的文档）整篇正文是 `<span data-as="p">` 靠 CSS 渲染成块。实测某文档页自动扫描覆盖率只有 10%——22 个候选里全是标题和列表项，一个正文段落都没有。现在按渲染盒子认段落，同一页覆盖率升到 96%。放宽的只是「什么可以当段落」，最短长度、英文占比、边栏排除、正文容器限制一条没松。

- **鼠标停在段落上按快捷键，却报「未找到可解析的段落」**。快捷键、右键、选中这类显式手势此前套用整页自动扫描那套取舍，而那套取舍是为了扫描时躲开边栏与样板文字，用在用户已经用鼠标指明的段落上只会误伤。四类页面因此整片失灵：多 `<article>` 的博客列表与论坛、SPA 换内容后（正文容器被缓存且从不失效，此后每次按键都失败，直到重新加载标签页）、短于 20 个字符的段落、段内夹了插图的段落。

- **解析极慢甚至整页没有译文**。思考模型会为一句话先生成上万 token 的推理：实测 `deepseek-v4-flash` 解析单句默认 153 秒 / 14789 token，超过 120 秒超时上限。而 DeepSeek 现存的两个模型都是思考模型，原先却要用户自己去选项页勾选「关闭模型思考」。现在默认要求模型不做思考，端点不接受该参数时自动去掉并重发，无需任何设置——同一句降到 1.41 秒 / 135 token。

- **点击成分看详解报「REQUEST_CANCELLED」，点「重新解析」毫无反应**。service worker 空闲约 30 秒即被终止，而标签页与文档的对应关系记在内存里，重启后会另生成一个 documentId，页面上已渲染卡片却还攥着旧的——于是详解、重试、纠正全被判成过期文档拒掉。改为记入 session 存储，熬过重启。

- **加粗标题解析后塌成正文字号**，整篇文章的层级消失。卡片插在原元素之后，继承的是父容器字体而不是被替换的那个元素。现在把原元素的字号与字重搬到卡片上，按层级等比缩放；普通段落不写死尺寸，继续跟随页面。

- **长页面主按钮永远停在「解析中…」**，不会变成「恢复网页原文」。完成判定此前要求所有已发现的句子都出结果，而屏外句子要滚动到可见才解析，长页面永远覆盖不满。改判定为「当前没有在飞的工作」。

- **切换页面后新页面被自动整篇翻译**。SPA 站点点链接走 `history.pushState`，不重载文档，会话于是活到下一个页面。现在地址变化即结束会话并通知页面停止。

- **弹窗开着时解析跑完，主按钮不刷新**。此前只在打开弹窗那一刻取一次状态，现在会话进行中每秒跟随刷新，弹窗关闭即停。

### 改进

- **解析明显变快**。实测耗时几乎只由模型输出的 token 数决定（首字节时间恒定约 0.65 秒，与输入大小无关）。此前模型吐的是带缩进的 JSON 还包在代码围栏里，一句话 466 字符中真正的信息不到三分之一；现在要求紧凑输出，输出 token 省 47%。同时按端点区分合批粒度：云端 API 拆小批并行（同样 6 句从 8.0 秒降到 3.1 秒），本地模型仍合并成大块——它串行处理请求，取舍相反。

- **首次解析不再拿用户等待的那次请求试错**。保存配置时就探明端点能力（是否接受 json_schema 等），此前要等第一次真实解析去撞一次 4xx 再降级。

## 1.0.6 — 2026-07-31

一项修复、一项新功能，都关于「指哪解析哪」这件事本身。

### 修复

- **鼠标停在段落上按快捷键，却报「未找到可解析的段落」**。快捷键、右键、选中这类用户显式手势，此前套用的是整页自动扫描那套取舍——而那套取舍是为了在扫描时躲开边栏与样板文字，用在用户已经用鼠标指明的段落上只会误伤。四类页面因此整片失灵：多 `<article>` 的博客列表与论坛、SPA 换内容后（正文容器被缓存且从不失效，此后每次按键都失败，直到重新加载标签页）、短于 20 个字符的段落、段内夹了插图的段落。

  另外，判断「这是不是一段」现在按渲染盒子而不是标签名。Mintlify 一类文档站（包括 Claude Code 自己的文档）整篇正文是 `<span data-as="p">` 靠 CSS 渲染成块，只认 `<p>` 会把这类站点整页判成「没有段落」。段内插图也不再否掉整段——卡片替换只是把原节点藏起来、退出时原样恢复，插图从来不妨碍可逆渲染。

  自动扫描的候选判定没有改变。

### 新增

- **正在解析的段落会自己亮出来**。此前只有右下角进度胶囊给一个全局计数，段落本身从按下快捷键到首个成分出现（可能数秒）没有任何变化，进度对不上具体哪一段。现在解析中的段落显示一根左侧竖条与极淡底色，流式渲染换成卡片后标记跟着走，成功、失败、停止、断连——任何一种收尾都立即撤掉。

  标记用 `inset box-shadow` 实现，不参与布局计算，所以文字不会因此位移。系统开启「减弱动态效果」时不做呼吸动画。

## 1.0.5 — 2026-07-27

一项性能改进。

### 改进

- **模型偶发把纯标点切成单独成分时，不再多跑一轮请求**。此前这类输出会让校验失败并触发一次完整的修复请求（本地模型上每轮 6–23 秒）；实测四句样本里会中一句。现在在校验前就地丢弃这类成分——语法规则本就允许标点不被覆盖，页面显示完全不受影响。

## 1.0.4 — 2026-07-27

两项性能改进，行为没有变化。

### 改进

- **同屏多个段落合并成一次请求**。此前每个段落单发一条，而一个段落常常只有 1–2 句，固定指令要在每条里重付一遍（单句请求里它占 82%）。实测一页 6 个段落从 6 条请求降到 1 条，输入长度省 69%。段落多的文档页整体明显变快，本地模型上尤其明显——它通常串行处理请求，减少请求数是最直接的杠杆。
- **详解面板改为流式**。点击成分后不再整段干等：结构逐条出现。实测本地 9B 上首个结构从 10.9 秒提前到 5.4 秒。端点不支持流式时自动改回整段返回。

### 已知限制补充

- 本地 Ollama 对 `qwen3.5` 架构强制串行处理请求（Ollama 侧的已知缺口），设置 `OLLAMA_NUM_PARALLEL` 无效。需要并发请换 `qwen3` / `qwen3moe` 架构的模型。

## 1.0.3 — 2026-07-26

修复 1.0.2 引入的一处回归。建议从 1.0.2 升级。

### 修复

- **长句折行时，详解面板重新出现在被点成分那一行的下方**。1.0.2 为了让面板不再挤动同行的邻句，把面板移到了句子外层，副作用是丢掉了句内按行定位——长句无论点第几行，面板都落到整句最后。现在按「被点成分所在行是否是整句最后一行」分流：长句折行时插在句内该行之后，短句共行时仍插在句子外层，两种场景各自正确。

## 1.0.2 — 2026-07-26

两处体验修复，无新增功能。

### 修复

- **没配模型时的进度提示不再说「解析中」**。纯缓存模式下这一趟根本没有模型请求，现在显示「查询缓存中 N/M」，结束时报「✓ 缓存命中 1/4」。同时修正了进度计数——此前未命中的句子（计为 skipped）没被算进已完成数，计数会一直停在 0，看着像卡死。
- **点开成分详解不再挤动同一行的其他句子**。此前打开详解会让所在句子变为块级独占一行，把与它共行的短句挤到下一行，看起来像"点一下上面的排版全变了"。现在详解面板作为句子的兄弟节点插在整行下方，行内文字一个字都不移动，面板照样以栏宽展示。

## 1.0.1 — 2026-07-26

只有图标与安装包的变化，功能与行为完全没有改动。1.0.0 已发布的产物保持原样不动。

### 变更

- **换用新图标**。原先是自制的成分色条图形，现在是句法树 + 放大镜 + 「语法」标签，主题更清楚。四个尺寸均为裁去白边、带透明圆角的 RGBA。
- **安装包瘦身**：排除 source map，未压缩体积从 693 KB 降到 188 KB。源码在 GitHub 上公开，需要调试的可以自行构建。

### 新增（面向维护者，不影响使用）

- 隐私政策 [PRIVACY.md](PRIVACY.md) 与应用商店上架资料 [chrome-plugin/docs/chrome-web-store.md](chrome-plugin/docs/chrome-web-store.md)。
- `npm run screenshots`：用本机真实模型生成商店截图。

## 1.0.0 — 2026-07-26

第一个正式版。

### 功能

- **按成分对齐的学习卡片**：把英文段落就地替换为「英文成分 + 角色标签 + 中文译文」三层对齐的卡片，点击任一成分展开详解。
- **模型配置**：支持任意 OpenAI 兼容端点（本地 Ollama、DeepSeek 等），多套配置可切换，连接可自测。
- **多种解析入口**：点击图标解析整页可视区域、右键解析选区或指定区域、`Alt+T` 解析鼠标悬停的段落（可作为页面冷启动入口）。
- **流式渐进渲染**：段落边生成边显示，成分逐个出现，不必等整段解析完。端点不支持流式时自动改回整段返回。
- **解析缓存**：按句文本缓存，跨页面与跨会话复用；可导出/导入，可设容量上限。无模型配置时进入纯缓存模式，只回显已缓存的解析。
- **纠错重解析**：对某句的解析不满意可提交说明重新解析，结果按反馈文本单独缓存。
- **成分详解预载**（可选）：每句解析完成后自动生成全部成分详解并入缓存。

### 隐私与权限

- 不申请常驻 host 权限；模型端点的访问权限在配置时按需申请，仅限所填 origin。
- 页面访问走 `activeTab`，只在用户主动触发时生效。
- 发给模型的只有待解析的句子文本与其 Token；API Key 与自定义请求头只存在本地 `chrome.storage.local`，且从不进入日志或渲染结果（响应中若混入凭据会被脱敏）。
- 内容脚本读不到扩展的受信存储。

### 已知限制

- **思考模型必须关闭思考**。Qwen3 一类模型默认会为单句生成上万字符推理（实测 246 秒），远超本扩展 120 秒的超时上限，表现为整页没有译文。请在选项页勾选「关闭模型思考」（实测同句降到 7.4 秒）。
- 本地模型空闲卸载后，第一次请求需要冷加载权重，可能超时一次，卡片会给重试按钮；可用 `OLLAMA_KEEP_ALIVE` 拉长驻留避免。
- 表单、可编辑区域、密码框所在的段落不会被解析。
- 单句规范化后超过 2,000 字符会被跳过。
- 仅通过 GitHub Release 分发，需要开发者模式加载，Chrome 会周期性提示「禁用开发者模式扩展」。
