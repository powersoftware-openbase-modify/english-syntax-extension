# 双击取词 + 生词本起点(免费功能)实现计划

## 已确认决策
- 数据:ECDICT(MIT)按词频裁剪 ~5 万高频词,**纯内置**扩展包,构建期与运行时均无网络依赖
- **权限取舍(有意识决策,不是零成本)**:常驻静态注入 `content_scripts`(http/https 全站)等效于安装时授予全站主机权限,CWS 安装警告升级为「读取和更改您在所有网站上的数据」,审核时长也会变长。不走 `activeTab`/`optional_host_permissions` 的原因:双击不是可申请权限的手势,且 `chrome.permissions.request` 只能在扩展上下文(popup/options)调用,content script 里弹不出授权框——常驻静态注入是唯一务实解,代价已认领并写入 `PRIVACY.md` 与 CWS 审核预期
- 交互:**任何网页常驻生效**(`<all_urls>` content script),双击单词触发
- UI:**极简悬浮条**——单词 + 音标 + 考试标签徽章 + 首条中文释义 + 发音按钮(Web Speech API)+ ☆ 收藏按钮;AI 仅限订阅用户的「深度讲解」按钮(⑪),不接其它实时 AI
- **本期范围(已扩大)**:基础三件套(双击取词 + 考试标签徽章 + 收藏按钮,生词本 store 一并建好)之外,订阅激活 + 托管模型、生词页面高亮回显、复习卡片(简化 SRS)、生词本导出 CSV、句子收藏本 + Anki 导出、点读模式、学习统计、整句朗读 + 跟读对照、**AI 单词深度讲解**全部并入本期;实施顺序与依赖见「本期实施顺序」一节
- 商业模式:取词、生词本永久免费;语法解析与 AI 深度讲解为订阅功能(按月收费,license-sdk 单独立项)。**免费/付费边界从第一天写进商店描述**;托管模型与密钥边界方案已预先定稿,见文末「订阅基建」一节

## 关键架构决策
1. **查词典不经过 SW,收藏必须经过 SW**:双击查询是纯 content 侧本地查词典,不发消息。但**收藏不能在 content script 里开 IndexedDB**——content script 运行在页面源上,那里的 IndexedDB 是页面的库(按站点隔离、清站点数据即丢、跨站不互通),不是扩展的库。收藏走 `chrome.runtime.sendMessage` 到 SW,由 SW 侧 store(AnalysisCache 同款 IndexedDB 模式)落库。这会新增 `RequestMessage`/`ResponseMessage` 成员——**协议三层校验同步生效**:`protocol.ts` 类型 + SW 侧校验/路由 + content 侧 `isRuntimeResponse` 的 switch case,三处缺一不可(漏 content 层 case 会把成功响应静默替换成 ERROR,缓存写对但计数全错,曾真机验收才暴露)。
2. **与句法功能零耦合**:不碰 `session-controller` / `BlockReplacement` / `learning-block`;悬浮条是独立 Shadow DOM host,挂在 `document.documentElement` 下,与现有卡片同样的挂载模式。双击监听用 capture 只读,不阻断页面行为。
3. **词典分片按需加载**:`<first-2-letters>.json.gz` 分片,首次查某词时 `fetch(chrome.runtime.getURL(...))` + `DecompressionStream("gzip")` 解压,内存 LRU 缓存已加载分片。避免启动时全量加载。**CSP 兜底**:部分站点用严格 CSP(connect-src)能拦 content script 的 fetch——分片加载失败时自动降级走 SW 消息取分片(SW fetch 扩展资源不受页面 CSP 约束),实现不得假设 fetch 一定成功。
4. **content script 读不到 chrome.storage(TRUSTED_CONTEXTS)**:v1 悬浮条不设任何配置项,常驻即用,绕开该约束。

## 词典数据管线(构建期一次性)
- 新增 `chrome-plugin/scripts/build-dict.mjs`:消费 ECDICT 的 `ecdict.csv`(手动下载,不进仓库),筛选规则:`bnc ≤ 40000 或 frq ≤ 40000 或 tag 含中考/高考/CET4/CET6/考研/TOEFL/IELTS`,去重后目标 ≤5.2 万词条
- 每条仅保留:`word`、`phonetic`、`translation`(截断到首行完整释义,≤120 字)、`exchange`(词形变化,供原形回退)、`pos`、`tag`(考试标签,仅保留筛选集合内的值:中考/高考/CET4/CET6/考研/TOEFL/IELTS/GRE,供悬浮条徽章)
- 输出:`chrome-plugin/public/dict/<shard>.json.gz`(按小写前两位分片)+ `dict/attribution.txt`(ECDICT MIT 声明 + 词数/版本戳)
- 生成的分片**提交进仓库**(预计 6~12MB,CWS 打包直接可用);数据更新 = 重跑脚本

## content 层新模块 `src/content/word-lookup/`
| 文件 | 职责 |
|---|---|
| `word-lookup.ts` | 入口:`installWordLookup(doc)` 注册 `dblclick`(capture),已安装幂等 |
| `word-selection.ts` | 从双击点提取单词:`selection.modify("extend","word")` 优先,`caretRangeFromPoint` 兜底;小写化、剥首尾标点;过滤非英文词(≥2 字符、仅字母/连字符/撇号);**先查 `event.composedPath()` 是否命中扩展自家 UI**(learning-block 卡片、word-popup 自身、进度胶囊的 host),命中即不响应,避免弹条叠在自家卡片上 |
| `lemma.ts` | 原形回退:先查 `exchange` 映射表,未命中再规则剥离(`-s/-es/-ed/-ing/-ly/-er/-est/-d`),回查词典 |
| `dictionary.ts` | 分片加载 + 内存缓存;`lookupWord(word): Promise<DictEntry \| undefined>`,内部尝试原词→原形 |
| `speech.ts` | `speak(word)`:选 `en-US` voice(优先 natural/local 优先级)、`rate 0.95`;每次先 `cancel`;`speechSynthesis` 不可用或无语音时返回 false(UI 据此隐藏发音按钮) |
| `word-popup.ts` | 极简悬浮条:Shadow DOM,`word + IPA + 考试徽章 + 释义首条 + 🔊 + ☆`;徽章渲染 `tag` 命中的考试标签;打开时先经消息查收藏态;☆ 点击后经消息落库并变实心,再点取消;定位在选区 rect 下方 8px,右侧越界左翻、底部越界上翻;外点 / `Escape` / 滚动 / 双击新词时关闭;`defer` 50ms 避免三击选段误触;**查无此词**(原形回退后仍未命中)时显示「词库未收录」态(仅单词 + 关闭),不出空卡 |
| `favorites-client.ts` | content 侧收藏消息封装:`isSaved(lemma)` / `save(entry)` / `unsave(lemma)`,**一律以 `lemma.ts` 归一后的原形为键**(词形表在词典分片里,查词时已顺手得到;SW 没有 exchange 数据,键归一不得下沉到 SW),经 `chrome.runtime.sendMessage`(content 可用 runtime 消息,读不到的只是 chrome.storage);sendMessage 失败静默降级为「未收藏」,不阻塞悬浮条 |
| `word-popup.css.ts` | 内联样式字符串(ts 导出),风格对齐现有卡片(深色浮层、小号字体) |

## background 层新增 `src/background/vocabulary-store.ts`
- 生词本 IndexedDB store,完全照 `analysis-cache.ts` 的模式(库版本化、单对象仓库);以**原形小写词**为键——收藏 `went` 落 `go`,查重也按原形,与 `lemma.ts` 的回退口径一致
- 数据结构:`{ lemma, word, phonetic, translation, tags, savedAt, reviewStage? }`;`reviewStage` 字段 v1 不写,为第二版简化 SRS(1/2/4/7/15 天)预留
- 新增协议消息:`SAVE_WORD` / `UNSAVE_WORD` / `GET_WORD_SAVED`(键为 content 侧传入的 lemma 原形,SW 直接落库不二次归一;`GET_WORD_SAVED` 返回 `{ saved, entry? }`)
- SW 路由:纯本地存储操作,不进 request-scheduler,不碰模型链路
- **v1 必须带查看入口**:popup 显示生词数并链接到选项页生词本列表(v1 仅列表 + 移除,复习归第二版)——只有悬浮条收藏、没有查看入口的话,「学习伴侣」闭环立不起来

## manifest 变更
```json
"content_scripts": [{
  "matches": ["http://*/*", "https://*/*"],
  "js": ["content-script.js"],
  "run_at": "document_idle"
}],
"web_accessible_resources": [{
  "resources": ["dict/*.json.gz", "dict/attribution.txt"],
  "matches": ["http://*/*", "https://*/*"],
  "use_dynamic_url": true
}]
```
- `content-script.ts` 末尾追加 `installWordLookup()`(幂等,与现有会话逻辑互不影响);静态注入的 IIFE 产物名与 `vite.content.config.ts` 的 `fileName: () => "content-script.js"` 一致,非 module 满足静态注入要求
- **`web_accessible_resources` 不可省略**:MV3 下 content script `fetch(chrome.runtime.getURL(...))` 的扩展资源必须声明,否则分片请求直接被拒;`use_dynamic_url: true` 让分片 URL 带动态 session ID,缓解「页面可探测扩展存在」的指纹面(WAR 暴露后任何页面都能 fetch 分片,数据无害但等于加了探测面)
- 现有 `activeTab`/`scripting`/`contextMenus` **全部保留**(句法功能照旧按需注入)
- 不新增任何 API 权限(但如上,常驻注入等效于安装时全站主机权限)
- **`manifest.test.ts` 同步更新**(必红项):`not.toHaveProperty("host_permissions")` 与 permissions 精确相等的断言要按新口径重写——新增 `content_scripts` 与 `web_accessible_resources` 的正向断言;这是更新口径,不是放宽
- **已知边界:iframe 内取词不生效**——`content_scripts` 默认只注顶层 frame,`all_frames: true` 会让每个 iframe 都注入整包,性能代价不值;v1 记为已知限制,用户反馈多再评估

## 测试(TDD)
- **vitest 单测**(happy-dom)。happy-dom 缺失三个 API,mock 策略先行定死:
  - `selection.modify("extend","word")` / `caretRangeFromPoint`:自造 Range/Selection stub(word-selection 测试)
  - `DecompressionStream`:`dictionary.test.ts` mock fetch 返回解压后对象 + mock DecompressionStream 直通,专注分片命中/未命中/缓存逻辑;真实 gzip 行为用一条 Node `zlib` 集成小测覆盖
  - `speechSynthesis`:整棵 mock(voice 选择、cancel 顺序、不可用降级)
- 覆盖:`word-selection`(双击目标/标点/数字过滤/**自家 UI 排除**)、`lemma`(exchange 命中、规则剥离、查无此词)、`dictionary`(分片命中/未命中/缓存/**fetch 失败走 SW 兜底**)、`speech`、`word-popup`(出现/定位翻转/三种关闭路径/新双击替换内容/徽章渲染/☆ 收藏态切换/**未收录态**)、`favorites-client`(消息成功/失败静默降级)、`vocabulary-store`(fake-indexeddb:落库/原形键查重/解除收藏)、`protocol.test.ts` 补三个新消息的守卫用例
- `build-dict.mjs` 按仓库 scripts 惯例配 `build-dict.test.mjs`(用小样本 CSV 断言筛词规则/字段裁剪/分片切分/tag 白名单),脚本本身进 lint 与 format 门禁
- **Playwright E2E**(`tests/e2e/word-lookup.spec.ts`):fixtures 页面双击单词 → 悬浮条出现且含音标;选一个带考试 tag 的词断言徽章出现;点 ☆ → 重开悬浮条收藏态保持(探针断言走 SW 存储,不用墙钟);点发音不抛错;点击外部关闭。现有 E2E 全部不得变红(双击监听只读,不影响既有用例)
- **门禁**:`npm test && npx playwright test && npm run lint && npm run format:check && npm run build`;lint 基线仍恰好 1 错误

## 文档与合规同步
- `PRIVACY.md` 重写披露:常驻 content script 注入所有网页(安装时授予全站主机权限),但**仅在双击时本地查询内置词典,不读取/上报任何页面内容、无网络请求**;生词本数据仅存扩展本地 IndexedDB,不离开设备;「零数据收集」结论不变;同时如实披露 WAR 暴露的分片资源可被页面 fetch
- 预期 CWS 审核因全站 content script 变长,发布排期留余量;审核表单需为全站 content script、WAR、storage 各权限填写用途说明(仅本地词典查询/生词本,无数据上传)
- `docs/architecture/protocol.md` 补 `SAVE_WORD` / `UNSAVE_WORD` / `GET_WORD_SAVED` 三个消息(architecture-docs.test.ts 钉消息类型全覆盖,漏写会红);`modules.md` 增补 word-lookup 与 vocabulary-store 各文件;`rendering.md` 加「双击取词悬浮条」与生词本一节;`build-test-release.md` 记录词典构建脚本与数据更新流程
- 仓库根 `CHANGELOG.md` 记条目

## 功能全景表(现有 + 规划)

面向学生与英语爱好者,按「免费引流」与「订阅价值」两组列示,工作量与版本节奏对照。**免费/付费边界从第一天写进商店描述**:取词、生词本、复习相关功能永久免费;语法解析与 AI 深度讲解为订阅功能。

| 功能 | 分组 | 版本节奏 | 工作量 | 说明 |
|---|---|---|---|---|
| 整页句法解析(段落对齐译文 + 进度胶囊 + 视口驱动) | 订阅 | **已上线**(订阅化后纳入月费) | — | 现有核心;订阅化后由托管模型提供额度 |
| 成分点击整句详解 + 流式渲染 | 订阅 | **已上线**(订阅化后纳入月费) | — | 现有核心 |
| 选中/悬停/右键解析手势 | 订阅 | **已上线**(订阅化后纳入月费) | — | 现有 |
| 本地解析缓存(IndexedDB) | 免费 | **已上线** | — | 现有;订阅化后仍按句缓存 |
| 双击取词悬浮条(音标 + 释义 + 发音) | 免费 | **第一版(本计划)** | 小 | 数据 ECDICT + Web Speech API,零网络依赖 |
| 考试词级标签徽章(中考/高考/CET4/CET6/考研/TOEFL/IELTS/GRE) | 免费 | **第一版(本计划)** | 近零 | ECDICT `tag` 字段纯展示,数据已在词包里 |
| 生词本(☆ 收藏 + popup 计数 + 选项页列表) | 免费 | **第一版(本计划)** | 小 | SW 侧 IndexedDB,原形为键;先只存不复习 |
| 生词页面高亮回显 | 免费 | **本期 ⑤** | 中 | 留存杀手锏:收藏词在后续网页虚线下划线,点击复习;MutationObserver 避开编辑框 |
| 生词复习卡片(简化 SRS) | 免费 | **本期 ⑥** | 中 | 5 档固定间隔(1/2/4/7/15 天),不上完整 FSRS |
| AI 单词深度讲解 | **订阅** | **本期 ⑪** | 中 | 词根词缀 + 近义辨析 + 分级例句;走现有模型链路 detail-click(1) 优先级、结果进缓存;「查词免费,讲透收费」的升级路径 |
| 订阅激活 + 托管模型(license-sdk + CF AI Gateway) | **订阅** | **本期 ④** | 中 | 方案已定稿,见文末「订阅基建」节;未决事项已升级为阻塞项 |
| 生词本导出 CSV | 免费 | **本期 ⑦** | 小 | 生词本数据结构已预留字段 |
| 句子收藏本 + Anki 导出 | 免费 | **本期 ⑧** | 小 | 三行对照卡片导出即现成学习材料;备考人群硬需求 |
| 点读模式(单击发音开关) | 免费 | **本期 ③** | 小 | 选项页开关,Web Speech 复用,亲子/基础场景 |
| 学习统计(查词/收藏/解析/连续天数) | 免费 | **本期 ⑨** | 小 | 本地计数放 popup,打卡心理 |
| 整句朗读 + 跟读对照 | 免费(跟读可作订阅权益) | **本期 ⑩** | 小 | 朗读 Web Speech;跟读模式逐成分高亮,对听力备考价值大 |

## 本期实施顺序(依赖驱动)

本计划正文仍是①②的详细设计;③以后各项开工前按项目流程各补 spec(`docs/superpowers/specs/`),不拿半页计划直接写代码。

| 步 | 功能 | 依赖 | 关键注意点 |
|---|---|---|---|
| ① | 词典构建 + 双击取词 + 考试徽章 | 无 | 本计划正文已详设 |
| ② | 生词本(store + 收藏 + popup 计数 + 选项页列表) | ①(lemma 键) | 本计划正文已详设 |
| ③ | 点读模式 | ① speech | **开关在 content 侧生效,但 content 读不到 chrome.storage(TRUSTED_CONTEXTS)**:options 页改开关后经 SW 广播到各标签页,content 收消息更新内存态;SW 启动时不主动推,首次响应取默认双击 |
| ④ | 订阅激活 + 托管模型 + BYOM 移除 | 未决事项拍板 + 平台产品配置(TRIAL_FIRST、trialCount、MONTHLY) | license-sdk 集成(拷 `webextension.js`)、Worker 验订阅、AI Gateway 接线、选项页改订阅卡片、协议新错误码三层同步 |
| ⑤ | 生词页面高亮回显 | ② | MutationObserver 轻量注入,避开编辑框;与自家卡片/悬浮条互不误伤 |
| ⑥ | 生词复习卡片(简化 SRS) | ② | `reviewStage` 字段已在 store 预留;复习页入口放 popup |
| ⑦ | 生词本导出 CSV | ② | 选项页导出按钮即可 |
| ⑧ | 句子收藏本 + Anki 导出 | 既有解析卡片 | 复用缓存中的句子/成分/译文数据,新 store + 导出 |
| ⑨ | 学习统计 | ①②③ | 事件计数在各功能落点埋,popup 展示;只本地存 |
| ⑩ | 整句朗读 + 跟读对照 | 既有卡片 + ③ speech | 朗读免费;跟读逐成分高亮,可标订阅权益 |
| ⑪ | AI 单词深度讲解 | ② 悬浮条 + ④ 订阅门禁 + 既有模型链路 | 词根词缀/近义辨析/三个难度分级例句;悬浮条加「深度讲解」按钮,订阅用户点击展开;新协议消息(如 ANALYZE_WORD_DETAIL)走**协议三层校验同步**;请求进 request-scheduler 套 detail-click(1) 优先级,结果照常进缓存;未订阅点按钮返回 LICENSE_REQUIRED 引导购买 |

## 明确不做(本期外)
- CDN 长尾词包、Azure TTS 音频(已定 Web Speech API,未来可作 Pro 升级选项)、WordNet 英文释义
- 悬浮条内 AI 内容仅限「深度讲解」按钮一项(订阅用户点击后展开),不做划词翻译等其它实时 AI 入口
- 完整 FSRS 算法(复习间隔保持 5 档固定)

## 未决事项(④ 订阅基建开工前必须拍板,已从非阻塞升级为阻塞)
- `productUniqueCode` 编码与 edition 层级(如 BASIC/PRO;TRIAL 是否单列)
- 试用扣减口径:建议每页会话(START_SESSION)扣 1 次 trialCount(版本级 20 次,平台不记消耗、插件本地扣减),重解析/详解点击不扣
- 计费周期确认:平台 MONTHLY 是一次性购买 30 天、非自动续费,是否接受(自动续费需另找方案)
- BYOM 存量迁移:现网用户已保存的自填模型 profile 在移除该表单后如何清理与告知
- 离线宽限:NETWORK_ERROR 时按最近一次 verify 结果放行的时间窗(建议 72h)
- machineLimit 引导文案与「解绑本设备」按钮的落位(选项页)

## 已定假设
- ECDICT CSV 由你手动下载到本地(构建脚本不联网,CI 不受影响)
- 生成分片**提交进仓库**(已定):CWS 打包、E2E、architecture-docs.test 都直接可用,不引入「先跑 build-dict 再测试」的构建顺序耦合;6~12MB git 体积可接受;`attribution.txt` 满足 ECDICT 的 MIT 署名义务

## 订阅基建:托管模型与密钥边界(为 license-sdk 立项预先定稿)

订阅模式意味着由我们提供模型额度,用户不再自填模型。本节定死密钥边界与网关选型,license-sdk 立项时直接引用,不再重新讨论。

### 密钥边界(三条铁律)
1. **DeepSeek key 只存服务端,永不进扩展**——扩展包人人可解包,写进常量、chrome.storage 默认值或选项页预填都等于公开,被扫到就是账单爆炸;
2. **扩展侧只有两个非秘密**:网关 base URL(公开常量,逆出来也没用,网关验订阅)+ 用户自己的 `licenseCode + activationToken`(按 license-sdk 规范存 local+sync);
3. **本地开发与真机验收**:key 只从环境变量读(`~/.secrets` 的 `DEEPSEEK_API_KEY`,`source ~/.secrets` 注入);脚本放 `.superpowers/acceptance/`(已 gitignore,永不提交),日志一律脱敏(`key <masked>`)。

### 网关链路(两层,各干各的)

```
扩展 SW(内置托管 profile,OpenAI 兼容 baseUrl 指向 Worker)
   → Worker(几十行:powersoftware-license-sdk 校验 licenseCode,失败 401;
            通过则注入 user_id custom metadata)
   → Cloudflare AI Gateway(BYOK 托管 DeepSeek key、Rate Limiting、
            Spend Limits 按用户预算桶、Caching、Analytics)
   → DeepSeek
```

- **分工**:AI Gateway 不认识 powersoftware 的授权体系,验订阅只能自己写——所以 Worker 只干「验 license + 打用户标」,AI Gateway 干「持钥、限流、按用户花费预算、缓存、对账」。
- **成本**:AI Gateway $0/月,免费档每天 10 万次网关请求、持久日志 10 万条/月;token 费用照常付给 DeepSeek;超规模再上 Workers Paid($5/月起)。
- **兼容性**:网关暴露 OpenAI 兼容 `/v1/chat/completions`,现有 `openai-compatible-adapter` 零改动;内置托管 `ModelProfile` 的 `apiKey` 字段复用为 license token——网关验的是订阅,不是 DeepSeek key。
- **缓存的额外价值**:同一英文句的 prompt 跨用户完全相同,AI Gateway 边缘缓存命中即零 token 成本;扩展本地缓存只挡单用户重复,这一层挡跨用户重复。

### 风险与对策
- **隐私口径必须改**:托管链路意味着用户句子流经我们,且 AI Gateway 默认记录请求内容(日志里能看到 prompt)——`PRIVACY.md` 的「零数据收集」必须改写为如实披露,或在网关上关闭日志存储收窄暴露面;国内用户受 PIPL 约束,句子内容属个人信息,这条比省钱重要。
- **国内访问**:Cloudflare 边缘国内可达但延迟波动;国内用户为主时评估阿里云 AI 网关(Serverless 免费公测,同为统一入口/限流/观测)做国内侧对位,国际流量走 Cloudflare。
- **流量隔离**:评测/验收流量直连 DeepSeek,不进网关,避免评测用量混进用户统计与花费预算。